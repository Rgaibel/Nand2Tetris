//Note this was new for me: enum creates a constant unchangeable value
//this is in a separate class so that both HackAssembler and Parse can access it

public enum InstructionType {
     A_COMMAND, C_COMMAND, L_COMMAND
 }
